const myConst = 2 * 2;
const myStr = `This string is a template literal, which means it can interpolate variables like this ${myConst}.

Template literals are also able to represent new lines without adding an newline character to the string.`;

console.log(myStr);
